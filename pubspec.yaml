name: image_hashing
description: A Dart library for generating perceptual hashes (AHash, PHash, DHash, WHash) from images, providing efficient single-image hash computation and comparison.
version: 0.1.0
repository: https://github.com/RequieMa/image_hashing
license: BSD-3-Clause
# documentation: https://pub.dev/documentation/your_package_name # 自动生成
issue_tracker: https://github.com/RequieMa/image_hashing/issues

environment:
  sdk: ^3.7.0

dependencies:
  path: ^1.8.0
  image: ^4.5.3
  dartcv4: ^1.1.3
  http: ^1.3.0
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

publish_to: none

topics: # 最多5个标签
  - photos
  - images
  - hash
  - dart
